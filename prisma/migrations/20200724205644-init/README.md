# Migration `20200724205644-init`

This migration has been generated by James Lee at 7/24/2020, 8:56:44 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."Account" (
"id" SERIAL,
"name" text  NOT NULL ,
"active" boolean  NOT NULL ,
"createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,
"updatedAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,
"deletedAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,
PRIMARY KEY ("id"))

CREATE TABLE "public"."User" (
"id" SERIAL,
"email" text  NOT NULL ,
"firstName" text  NOT NULL ,
"lastName" text  NOT NULL ,
"active" boolean  NOT NULL ,
"createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,
"updatedAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,
"deletedAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,
PRIMARY KEY ("id"))

CREATE TABLE "public"."AccountUser" (
"id" SERIAL,
"accountId" integer  NOT NULL ,
"userId" integer  NOT NULL ,
"role" text  NOT NULL ,
"active" boolean  NOT NULL ,
"createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,
"updatedAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,
"deletedAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,
PRIMARY KEY ("id"))

CREATE TABLE "public"."Project" (
"id" SERIAL,
"accountId" integer  NOT NULL ,
"active" boolean  NOT NULL ,
"createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,
"updatedAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,
"deletedAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,
PRIMARY KEY ("id"))

CREATE TABLE "public"."Monitor" (
"id" SERIAL,
"name" text  NOT NULL ,
"region" text  NOT NULL ,
"url" text  NOT NULL ,
"assertion" text  NOT NULL ,
"interval" integer  NOT NULL ,
"projectId" integer  NOT NULL ,
"active" boolean  NOT NULL ,
"createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,
"updatedAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,
"deletedAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,
PRIMARY KEY ("id"))

CREATE TABLE "public"."MonitorResult" (
"id" SERIAL,
"monitorId" integer  NOT NULL ,
"success" boolean  NOT NULL ,
"response_time" integer  NOT NULL ,
"createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,
PRIMARY KEY ("id"))

CREATE UNIQUE INDEX "User.email" ON "public"."User"("email")

ALTER TABLE "public"."AccountUser" ADD FOREIGN KEY ("accountId")REFERENCES "public"."Account"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "public"."AccountUser" ADD FOREIGN KEY ("userId")REFERENCES "public"."User"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "public"."Project" ADD FOREIGN KEY ("accountId")REFERENCES "public"."Account"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "public"."Monitor" ADD FOREIGN KEY ("projectId")REFERENCES "public"."Project"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "public"."MonitorResult" ADD FOREIGN KEY ("monitorId")REFERENCES "public"."Monitor"("id") ON DELETE CASCADE ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20200724205644-init
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,99 @@
+// This is your Prisma schema file,
+// learn more about it in the docs: https://pris.ly/d/prisma-schema
+
+datasource db {
+  provider = "postgresql"
+  url = "***"
+}
+
+generator client {
+  provider = "prisma-client-js"
+}
+
+model Account {
+  id   Int    @default(autoincrement()) @id
+  
+  name String
+
+  projects Project[]
+  users    AccountUser[]
+
+  active    Boolean
+  createdAt DateTime @default(now())
+  updatedAt DateTime @default(now())
+  deletedAt DateTime @default(now())
+}
+
+model User {
+  id        Int    @default(autoincrement()) @id
+
+  email     String @unique
+  firstName String
+  lastName  String
+
+  accounts AccountUser[]
+
+  active    Boolean
+  createdAt DateTime @default(now())
+  updatedAt DateTime @default(now())
+  deletedAt DateTime @default(now())
+}
+
+model AccountUser {
+  id Int @default(autoincrement()) @id
+
+  accountId Int
+  account   Account @relation(fields: [accountId], references: [id])
+  userId    Int
+  user      User    @relation(fields: [userId], references: [id])
+  role      String
+
+  active    Boolean
+  createdAt DateTime @default(now())
+  updatedAt DateTime @default(now())
+  deletedAt DateTime @default(now())
+}
+
+model Project {
+  id Int @default(autoincrement()) @id
+
+  accountId Int
+  account   Account   @relation(fields: [accountId], references: [id])
+  monitors  Monitor[]
+
+  active    Boolean
+  createdAt DateTime @default(now())
+  updatedAt DateTime @default(now())
+  deletedAt DateTime @default(now())
+}
+
+model Monitor {
+  id Int @default(autoincrement()) @id
+
+  name      String
+  region    String
+  url       String
+  assertion String
+  interval  Int
+
+  projectId Int
+  project   Project         @relation(fields: [projectId], references: [id])
+  results   MonitorResult[]
+
+  active    Boolean
+  createdAt DateTime @default(now())
+  updatedAt DateTime @default(now())
+  deletedAt DateTime @default(now())
+}
+
+model MonitorResult {
+  id Int @default(autoincrement()) @id
+
+  monitorId Int
+  monitor   Monitor @relation(fields: [monitorId], references: [id])
+
+  success       Boolean
+  response_time Int
+
+  createdAt DateTime @default(now())
+}
```


